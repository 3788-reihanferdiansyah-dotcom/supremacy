export function DataSourceNote() {
  return (
    <p className="mt-1 text-xs text-current">
      This public page uses the bundled static attendance snapshot, not real-time
      auto refresh.
    </p>
  );
}
