import React from 'react';
import ReactDOM from 'react-dom/client';
import Page from '@/routes/page';
import '../recovery/static/css/main.ff1f0c091c.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Page />
  </React.StrictMode>,
);
